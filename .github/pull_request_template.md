## Summary

Describe the problem and the intended behavior change.

Related issue: <!-- e.g. Closes #123 -->

## Notes

- Run `make test` and `make lint` before opening the pull request.
- When a mermaid builder changes, run `make generate` and `make render-check`; the latter draws every committed diagram with the real renderer.
- Within v1.x nothing exported is removed, renamed or re-signatured, and generated output only changes when the current output is objectively broken.
